mixin navlink(href, img, className="link__img-wrapper", classLink="nav__link")
    a(href= href class= classLink)
        div(class= className)
            img.link__img(src="./img/icons/" + img alt= img)
        block
